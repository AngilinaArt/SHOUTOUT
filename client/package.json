{
  "name": "shoutout-client",
  "version": "0.1.0",
  "description": "Desktop notification system with hamster overlays and toast messages",
  "author": "Shoutout Team",
  "private": true,
  "main": "main.js",
  "type": "commonjs",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder",
    "build:win": "electron-builder --win --x64",
    "build:mac": "electron-builder --mac",
    "build:mac:x64": "electron-builder --mac --x64",
    "build:mac:arm64": "electron-builder --mac --arm64",
    "build:linux": "electron-builder --linux"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "7zip-bin": "^5.2.0",
    "electron": "31.3.1",
    "electron-builder": "^23.6.0"
  },
  "build": {
    "appId": "com.shoutout.app",
    "productName": "Hamster & Toast",
    "asar": true,
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    },
    "files": [
      "main.js",
      "preload.js",
      "preload_compose.js",
      "preload_name.js",
      "preload_status.js",
      "preload_reaction.js",
      "preload_userlist.js",
      "renderer/**",
      "assets/**",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "assets",
        "to": "assets",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "icon": "assets/icon/hamster.ico",
      "target": [
        "nsis"
      ],
      "artifactName": "${productName}-${version}-win.${ext}"
    },
    "mac": {
      "category": "public.app-category.utilities",
      "icon": "assets/icon/hamster.png",
      "target": [
        "dmg"
      ],
      "artifactName": "${productName}-${version}-mac.${ext}",
      "hardenedRuntime": true,
      "gatekeeperAssess": false
    }
  }
}
